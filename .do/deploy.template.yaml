  spec:
    name: pydacc
    services:
    - name: pydacc
      git:
        branch: batmanscode-digitalocean-1
        repo_clone_url: https://github.com/batmanscode/pydacc.git
      http_port: 8080
      environment_slug: python
      routes:
      - path: /
      run_command: gunicorn --worker-tmp-dir /dev/shm --config gunicorn.conf.py api:app
